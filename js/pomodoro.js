class Pomodoro {
  constructor(displayId, statusId) {
    this.display = document.getElementById(displayId);
    this.status = document.getElementById(statusId);
    // Gán giá trị từ input
  }

  start() {
    // setInterval countdown
  }

  pause() {
    // clearInterval
  }

  reset() {
    // về 25:00
  }
}